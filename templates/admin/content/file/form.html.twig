{% extends 'admin/admin.html.twig' %}

{% import 'admin/macro.html.twig' as macro %}

{% form_theme formView 'bootstrap_3_horizontal_layout.html.twig' %}

{% block title %}
    {% if file.id %}
        {{ 'admin.title.file.update'|trans({'%file%':file.label}) }}
    {% else %}
        {{ 'admin.title.file.create'|trans }}
    {% endif %}
    - {{ parent() }}
{% endblock %}

{% block adminMenu %}
    {% include 'admin/adminMenu.html.twig' with {activeMenu:'file'} %}
{% endblock %}

{% block buttons %}
    {{ macro.button('app_admin_content_file_list', 'admin.link.back_to_list'|trans) }}
{% endblock %}

{% block contentTitle %}
    {% if file.id %}
        {{ 'admin.title.file.update'|trans({'%file%':file.label}) }}
    {% else %}
        {{ 'admin.title.file.create'|trans }}
    {% endif %}
{% endblock %}

{% block adminContent %}

    {% if file.storageName is not null and file.storageName is not empty %}
        <img src="{{ uploadsDir ~ file.storageName }}" style='max-height:200px;' class="product-select-image" />
    {% endif %}

    {{ form_start(formView) }}
    {{ form_widget(formView) }}
    {{ form_end(formView) }}

    <script type="text/javascript">
    </script>

{% endblock %}

{% block preScripts %}
    {{ parent() }}
{% endblock %}

{% block postScripts %}
    {{ parent() }}
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}