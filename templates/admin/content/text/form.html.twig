{% extends 'admin/admin.html.twig' %}

{% block title %}
    Szoveges tartalom ({{ text.code }}) modositas - {{ parent() }}
{% endblock %}

{% block adminContent %}

    {{ form_start(formView) }}
    {{ form_widget(formView) }}
    {{ form_end(formView) }}

    <a href="{{ path('app_admin_content_text_list') }}">Vissza a tartalmak listahoz</a>

    <script type="text/javascript">
    </script>

{% endblock %}

{% block preScripts %}
    {{ parent() }}

    <script type="text/javascript" src="{{ asset('ckeditor/ckeditor.js') }}"></script>
{% endblock %}

{% block postScripts %}
    {{ parent() }}

    <script type="text/javascript">

        CKEDITOR.replace('text_text', {
            filebrowserBrowseUrl: '{{ path("app_admin_content_file_browse") }}',
            filebrowserUploadUrl: '{{ path("app_admin_content_file_upload") }}'
        });

    </script>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}