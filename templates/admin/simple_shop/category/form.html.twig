{% extends 'admin/admin.html.twig' %}

{% block title %}
    {% if category.label %}
        Kategoria ({{ category.label }}) modositas
    {% else %}
        Kategoria letrehozas
    {% endif %}
    - {{ parent() }}
{% endblock %}

{% block adminContent %}

    {{ form_start(formView) }}
    {{ form_widget(formView) }}
    {{ form_end(formView) }}

    <a href="{{ path('app_admin_simpleshop_category_list') }}">Vissza a kategoria listahoz</a>

    <script type="text/javascript">
    </script>

{% endblock %}

{% block preScripts %}
    {{ parent() }}
{% endblock %}

{% block postScripts %}
    {{ parent() }}
{% endblock %}

{% block stylesheets %}
     {{ parent() }}
 {% endblock %}