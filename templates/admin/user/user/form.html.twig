{% extends 'admin/admin.html.twig' %}

{% import 'admin/macro.html.twig' as macro %}

{% form_theme formView 'bootstrap_3_horizontal_layout.html.twig' %}

{% block title %}
    {% if user.id %}
        {{ 'page_title.admin.user.update'|trans({'%user%':user.name}) }}
    {% else %}
        {{ 'page_title.admin.user.create'|trans }}
    {% endif %}
    - {{ parent() }}
{% endblock %}

{% block adminMenu %}
    {% include 'admin/adminMenu.html.twig' with {activeMenu:'user'} %}
{% endblock %}

{% block buttons %}
    {{ macro.button('app_admin_user_user_list', 'common.back_to_list'|trans) }}
{% endblock %}

{% block contentTitle %}
    {% if user.id %}
        {{ 'page_title.admin.user.update'|trans({'%user%':user.name}) }}
    {% else %}
        {{ 'page_title.admin.user.create'|trans }}
    {% endif %}
{% endblock %}

{% block adminContent %}

    {{ form_start(formView) }}
    {{ form_widget(formView) }}
    {{ form_end(formView) }}

    <script type="text/javascript">
    </script>

{% endblock %}

{% block preScripts %}
    {{ parent() }}
{% endblock %}

{% block postScripts %}
    {{ parent() }}
{% endblock %}

{% block stylesheets %}
     {{ parent() }}
 {% endblock %}